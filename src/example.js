// Fixed linting errors
// Removed unused variable
console.log('This string uses single quotes now'); // Fixed: single quotes instead of double
let missingSemicolon = 42; // Fixed: added semicolon
function functionWithoutSemicolon() {
  return 'This return statement has a semicolon now'; // Fixed: single quotes + added semicolon
}